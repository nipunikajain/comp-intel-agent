# Copy to .env.local and set your values.
#
# Access code for the app (password gate). Users must enter this to see the app.
NEXT_PUBLIC_ACCESS_CODE=your-secret-code-here
#
# When the backend uses API_ACCESS_KEY, the frontend must send the same value
# in the X-Access-Key header. Set this to match backend API_ACCESS_KEY.
NEXT_PUBLIC_API_ACCESS_KEY=your-api-secret-here
#
# Optional: backend URL (default: http://localhost:8000)
#NEXT_PUBLIC_API_URL=http://localhost:8000
